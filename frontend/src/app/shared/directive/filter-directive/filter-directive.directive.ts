import {
  Directive,
  ElementRef,
  Input,
  OnChanges,
  Renderer2,
  SimpleChanges,
} from '@angular/core';

@Directive({
  selector: '[appFilterDirective]',
  standalone: true,
})
export class FilterDirectiveDirective {
  
}
