import { NewsDirectiveDirective } from './news-directive.directive';

describe('NewsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
